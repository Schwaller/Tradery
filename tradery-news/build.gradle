// tradery-news: News intelligence module
// Fetches, parses, and structures crypto news from various sources

plugins {
    id 'java-library'
    id 'application'
    id 'org.beryx.jlink'
}

application {
    mainClass = 'com.tradery.news.ui.IntelFrame'
    applicationDefaultJvmArgs = ['-Dapple.awt.application.name=Intelligence']
}

dependencies {
    // JSON/YAML serialization
    api "com.fasterxml.jackson.core:jackson-databind:${rootProject.ext.jacksonVersion}"
    api "com.fasterxml.jackson.datatype:jackson-datatype-jsr310:${rootProject.ext.jacksonVersion}"
    api "com.fasterxml.jackson.dataformat:jackson-dataformat-yaml:${rootProject.ext.jacksonVersion}"

    // HTTP client
    implementation "com.squareup.okhttp3:okhttp:${rootProject.ext.okhttpVersion}"

    // RSS parsing
    implementation 'com.rometools:rome:2.1.0'

    // HTML parsing (for content extraction)
    implementation 'org.jsoup:jsoup:1.18.3'

    // SQLite storage
    implementation "org.xerial:sqlite-jdbc:${rootProject.ext.sqliteVersion}"

    // Logging
    implementation "org.slf4j:slf4j-api:${rootProject.ext.slf4jVersion}"
    runtimeOnly "org.apache.logging.log4j:log4j-api:${rootProject.ext.log4jVersion}"
    runtimeOnly "org.apache.logging.log4j:log4j-core:${rootProject.ext.log4jVersion}"
    runtimeOnly "org.apache.logging.log4j:log4j-slf4j2-impl:${rootProject.ext.log4jVersion}"

    // License and update checking
    implementation project(':tradery-license')

    // AI infrastructure
    implementation project(':tradery-ai')
    implementation project(':tradery-ai-pipeline')

    // UI
    implementation project(':tradery-ui-common')
    implementation project(':tradery-help')
    implementation 'com.formdev:flatlaf:3.4'
    implementation 'com.formdev:flatlaf-intellij-themes:3.4'

    // Optional sharing support (loaded via reflection at runtime)
    runtimeOnly project(':tradery-documents')
    runtimeOnly project(':tradery-sharing')
}

// jlink configuration for creating minimal custom JRE
jlink {
    moduleName = 'com.tradery.news'
    mergedModuleName = 'com.tradery.news.merged'

    options = [
        '--strip-debug',
        '--compress', 'zip-9',
        '--no-header-files',
        '--no-man-pages',
        // Force-include reflection-loaded modules and crypto (not reachable from module graph)
        '--add-modules', 'com.tradery.sharing,com.tradery.documents,java.net.http,jdk.crypto.ec,jdk.crypto.cryptoki'
    ]

    // Add transitive dependencies that might be missing
    addExtraDependencies("jackson")
    addExtraDependencies("yaml")

    launcher {
        name = 'intelligence'
        jvmArgs = [
            '-Xmx512m',
            '-Dapple.awt.application.name=Intelligence',
            "-Dtradery.version=${project.version}",
            // Required for Jackson reflection on modular code
            '--add-opens', 'com.tradery.news/com.tradery.news.model=com.fasterxml.jackson.databind',
            '--add-opens', 'com.tradery.news/com.tradery.news.topic=com.fasterxml.jackson.databind',
            // Force sharing+documents into module graph (runtimeOnly deps, loaded via reflection)
            '--add-modules', 'com.tradery.sharing,com.tradery.documents',
            // Allow com.tradery.news to load sharing module via reflection
            '--add-reads', 'com.tradery.news=com.tradery.sharing',
            '--add-opens', 'com.tradery.sharing/com.tradery.sharing=com.tradery.news',
            // Sharing needs okhttp3/slf4j which are force-merged into the merged module
            '--add-reads', 'com.tradery.sharing=com.tradery.news.merged'
        ]
    }

    // Merge non-modular JARs into a single module
    mergedModule {
        additive = true

        // ServiceLoader declarations for Log4j2 (SLF4J auto-detected)
        uses 'org.apache.logging.log4j.spi.Provider'
        uses 'org.apache.logging.log4j.util.PropertySource'
        uses 'org.apache.logging.log4j.core.util.ContextDataProvider'
        uses 'org.apache.logging.log4j.message.ThreadDumpMessage.ThreadInfoFactory'
    }

    // Force merge non-modular/automatic module dependencies
    forceMerge('rome', 'jdom2', 'jsoup', 'okhttp', 'okio', 'slf4j', 'log4j', 'sqlite')

    jpackage {
        imageName = 'Intelligence'
        installerName = 'Intelligence'
        appVersion = project.version

        // macOS
        if (org.gradle.internal.os.OperatingSystem.current().isMacOsX()) {
            icon = 'src/main/resources/icon.icns'
            installerType = 'dmg'
        }
        // Windows
        if (org.gradle.internal.os.OperatingSystem.current().isWindows()) {
            icon = 'src/main/resources/icon.ico'
            installerType = 'msi'
            winMenu = true
            winShortcut = true
        }
        // Linux
        if (org.gradle.internal.os.OperatingSystem.current().isLinux()) {
            icon = 'src/main/resources/icon.png'
            installerType = 'deb'
        }
    }
}

// Packaging configuration for macOS signing and notarization
ext.packagingAppName = 'Intelligence'
ext.packagingDmgName = 'Intelligence'
apply from: "${rootProject.projectDir}/gradle/packaging.gradle"

// signApp depends on jpackage (jlink-based)
tasks.named('signApp') { dependsOn 'jpackage' }
